# コントリビュートガイド

etoki への変更は Pull Request で受ける。**ブランチを切る前にここを読む。**

このファイルはブランチ・コミット・PR・レビュー対応の**正本**。設計と実装の規約は
別で、[`CLAUDE.md`](CLAUDE.md) と各ディレクトリの `CLAUDE.md`（`internal/` /
`web/` / `api/`）にある。**書いていないことはそちらを見る。**

## はじめに

**判断で迷ったら中核となる思想に戻る。** 3 つとも
[README](README.md#中核となる思想) にある。何を作るかも、PR で何を説明すべきかも、
だいたいここで決まる。

過去の設計判断は [`docs/adr/`](docs/adr/) にある。**「なぜこうなっているのか」を
知らないまま直すと、記録された判断を黙って覆すことになる。** 触るところに関係する
ADR があるかは、先に索引（[`docs/adr/README.md`](docs/adr/README.md)）で見る。

## 開発環境

[Nix](https://nixos.org/) 以外はインストール不要。手順は
[README の「開発」](README.md#開発)。

**`make` を通す限り devShell に入り忘れても構わない**（自分をやり直す）が、
**`go test` や `bunx vitest` を直に叩く経路は包まれない。** そちらは
`nix develop` の中で実行する。

## ブランチ運用

**必ず `main` を起点にブランチを切る。`main` に直接コミットしない。**

```sh
git switch main && git pull
git switch -c <prefix>/<短い説明>
```

`<prefix>` はコミットの種別に合わせる（`feat` / `fix` / `docs` / `build` / `ci` /
`chore` / `refactor` / `test`）。

## コミット

- 1 コミットの粒度を小さく保つ。
- Conventional Commits 形式（`feat(scope): 要約`）。要約は英語、本文は日本語。
- **本文には「何をしたか」ではなく「なぜそうしたか」を書く。** 差分を読めば分かる
  ことは書かない。
- **常に緑のコミットにする。** テストだけ先にコミットして赤い状態を履歴に残さない。

## コミット前に通すもの

`make lint` と `make test`。UI かハンドラを触ったなら `make test-e2e` も通す
（`make test` には含まれない）。各ターゲットが何をするかは
[README の「開発」](README.md#開発)。

**`api/openapi.yaml` を変えたら `make codegen` を実行し、生成物を同じコミットに
含める。** 生成物は手で編集しない。CI の codegen drift ジョブが、再生成していない
状態とコミットし忘れの両方を落とす（[ADR 0011](docs/adr/0011-openapi-as-contract-ssot.md)）。

## 実装後のセルフレビュー

実装が一段落したら、**コミット前に `/rv` を実行する。** 差分を規約と中核思想に
照らして点検し、問題があれば直して再度点検する。手順と観点は
[`.claude/skills/rv/SKILL.md`](.claude/skills/rv/SKILL.md)。

- **サイクルは最大 3 周。** 3 周して残った指摘は直さずに報告して判断を仰ぐ。同じ
  箇所を 3 周直して収まらないなら設計の問題であり、手直しでは解けない。
- 指摘ゼロならその周で終える。周回数を消化しない。

## Pull Request を出す

### タイトル

コミットと同じ Conventional Commits 形式だが、**要約は日本語**にする。一覧で読む
ものなので、変更の中身より「何が変わるか」が伝わる書き方を選ぶ。

```text
feat(web): 作るものを選び、手直しできるようにする
fix: ブラウザ由来の cross-site リクエストを Host と Origin で弾く
```

### 本文

**[`.github/pull_request_template.md`](.github/pull_request_template.md) の見出しに
沿って埋める。** GitHub の Web UI では新規 PR の本文に自動で入る。**該当しない節は
消す。** 空の見出しを残すと、書き忘れたのか無いのかが読み手に分からない。

各節に何を書くかは以下のとおり。共通するのは 1 つで、**差分を読めば分かることは
書かない。** レビュアーが差分から復元できないのは「なぜそうしたか」と「採らな
かった案」だけで、PR 本文はそこに紙面を使う。

#### 何が困っていたか

**「機能が無い」ではなく「無いことで誰が何を失うか」を書く。** 中核となる思想や
ADR のどれに反していたかを名指しする。ここが埋まらないなら、その変更が要る理由が
まだ言語化できていない。

#### やったこと

**非自明な判断ごとに理由を添える。** 実装の一覧ではなく、判断の一覧にする。

採らなかった案があれば、なぜ採らなかったかも書く。**後から「なぜこうしなかったの
か」を聞かれる案は、だいたい検討済みで却下したもの**であり、それが残っていないと
同じ議論をやり直すことになる。

#### 判断の記録

**設計判断を下したなら [`docs/adr/`](docs/adr/) に記録し、PR から指す。** 実装を
読んでも分からない「なぜそうしたか」だけを書く。

**ADR 番号は他の PR と衝突しうる。** 同時期に進んでいる PR がすでに次の番号を
採っていないか確認する。あわせて `docs/adr/README.md` は両方が末尾に 1 行足すので
軽い衝突が出る。**先に気づいて PR 本文に書いておけば、マージする側が驚かない。**

#### テスト

**層ごとに何を固定したかを書く。** 件数だけでは、通っていることしか伝わらない。
回帰止めなら**どこが切れると落ちるか**を書く。それが無いと、後からそのテストを
消してよいかを誰も判断できない。

`make lint` / `make test` / `make test-e2e` の結果と件数を添える。**回していない
ものは「回していない」と書く。** 黙って省くと、通ったものと区別が付かない。

#### 画面

UI を触ったならスクリーンショットを添える。`make test-e2e` が
`web/e2e-output/screenshots/` に書き出すので、そのパスを指してよい（詳細は
[`web/CLAUDE.md`](web/CLAUDE.md)）。

**UI を触ったのに添えないなら、添えない理由を書く。** ネイティブの確認ダイアログの
ように撮れないものもある。節ごと消すのは、UI をまったく触っていないときだけ。

#### 確認していないこと

**通していない検証と、人にやってほしい確認を正直に書く。** 手元で動かせなかった
ツール、実物のサービスに繋いでいない部分、CI の結果を見てほしいところ。

**draft issue の作成は取り消せない**（[ADR 0009](docs/adr/0009-partial-creation-is-recorded.md)）。
作成に関わる変更では、**GitHub の実物に作らせた確認をしたか否かを必ず書く。**
していないなら、していないと書く。

#### 残した制約 / 宿題

今回入れなかったもの、別 issue に送るもの。無ければ節ごと消す。

## レビュー対応

PR を作ると CodeRabbit のレビューが付く。**作りっぱなしにせず、指摘がなくなるまで
対応する。** 手順は `/pr-review`（[`.claude/skills/pr-review/SKILL.md`](.claude/skills/pr-review/SKILL.md)）。

「指摘がなくなるまで」は、全部そのとおりに直すという意味ではない。**リポジトリの
規約や仕様と衝突する指摘は直さず、理由をコメントで返す。** レビュアーは事情を
知らない。どちらにせよ**黙って閉じない。**

## CI

[`.github/workflows/ci.yml`](.github/workflows/ci.yml) が PR ごとに走る。

| ステップ                                    | 中身                                                                   |
| ------------------------------------------- | ---------------------------------------------------------------------- |
| `nix flake check`                           | Nix コードのフォーマット検査（[ADR 0002](docs/adr/0002-toolchain.md)） |
| `setup` / `lint` / `test` / `e2e` / `build` | 手元と同じ `make` ターゲット                                           |
| `codegen drift`                             | 生成物が `api/openapi.yaml` と一致し、コミットされていること           |

**CI は手元と同じ入口（`nix develop --command make …`）を通る。** CI 専用の
セットアップ手順を持たないことで、「手元では通るが CI で落ちる」の原因を devShell の
外に閉じ込めている。落ちたら手元で同じ `make` を叩けば再現する。

e2e が落ちたときは Playwright のレポートが artifact（`playwright-report`）に
上がる。
