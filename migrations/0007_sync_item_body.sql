-- 作成した draft issue の本文を、作成時のスナップショットとして持つ（ADR 0022）。
--
-- title と違って body はこれまで記録していなかった。GitHub → etoki の逆方向
-- 同期は実装しないので（スコープ外）、作成時に控えなければ後から取り直せない。
-- 作成は取り消せない操作であり（ADR 0009）、何を作ったのかを etoki 側から
-- 追えないままにしない。
--
-- 空文字は「本文を記録していなかった頃の run」と「本文が空のまま作った item」
-- の両方を表す。区別しないのは、どちらも「読むものが無い」で画面上は同じ
-- 扱いになるため。
ALTER TABLE sync_items ADD COLUMN body TEXT NOT NULL DEFAULT '';
