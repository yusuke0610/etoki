// Package port は etoki が外部サービスに触れるための境界を定義する。
//
// このパッケージは internal/ の外に置かれており、モジュールの公開 API である。
// クラウド固有の認証が必要な環境では、利用者がここのインターフェースを実装し、
// 自前の main から差し込むことを想定している。判断の経緯は
// docs/adr/0001-dependency-direction-and-ports.md を参照。
//
// そのため port は internal/ の型に依存しない。境界をまたぐ値はこのパッケージが
// 自前で定義し、ドメインモデルとの詰め替えはユースケース層が行う。
package port
